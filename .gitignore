# OS / editor
Thumbs.db
.DS_Store
*.log

# Node (only if tooling is added later)
node_modules/
.cache/
.parcel-cache/
dist/

# Local env
.env
.env.*
!.env.example

# Claude / preview tooling
.claude/
.preview/

# Confidential source docs — keep local, never publish
*.pdf
.pdfpages/

# Large source image — ship the optimized assets/hero-nurse.jpg instead
nurse-saving-leaf.png

# Wrangler local cache
.wrangler/

# Large source images (ship optimized assets/* instead)
full-width-nurse-saving-leaf.png
